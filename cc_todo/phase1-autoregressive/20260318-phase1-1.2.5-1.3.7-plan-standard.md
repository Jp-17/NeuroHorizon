# Phase 1.2.5 / 1.3.7 plan.md 标准补写

## 日期

- 2026-03-18 16:43

## 对应任务

- `1.2.5` baseline_v2 评估协议复核与完成项补记
- `1.3.7` NeuroHorizon 实验默认数据与指标标准

## 任务目标

1. 根据 `cc_todo/20260316-review/20260316-plan-md-v2-code-review_codex.md` 的审查结论，把已经落地的 baseline_v2 评估协议修正补写回 `cc_core_files/plan.md`
2. 结合 `1.3.5` 与 `1.3.6` 的实现与结果，把 NeuroHorizon 后续实验默认使用的数据处理、sampler 与指标口径写成统一标准
3. 明确 `1.3.5` 与 `1.3.6` 是否遵循上述最新标准，避免后续再把 comparison metric、trial 指标和主 `fp-bps` 口径混写

## 执行命令

```bash
cd /root/autodl-tmp/NeuroHorizon

sed -n '220,340p' cc_core_files/plan.md
sed -n '386,442p' cc_core_files/plan.md
sed -n '1,260p' cc_todo/20260316-review/20260316-plan-md-v2-code-review_codex.md
sed -n '1,260p' cc_todo/20260316-review/QA_codex.md
sed -n '1,320p' examples/neurohorizon/train.py
sed -n '1,320p' scripts/analysis/neurohorizon/eval_phase1_v2.py
sed -n '1,320p' torch_brain/utils/neurohorizon_metrics.py
grep -rn $'\357\277\275\357\277\275' cc_core_files/ cc_todo/phase*/
```

## 本次修改

1. 在 `cc_core_files/plan.md` 新增 `1.2.5`：
   - 把 baseline_v2 review 中已经落地的两项核心修正写为已完成事项：
     - continuous eval 改用 deterministic `SequentialFixedWindowSampler`
     - continuous 主 `fp-bps` / `R-squared` 改为全局累计版
   - 同时把主 `fp-bps` 的正式口径收口为 `global spike-weighted + train-split null`
   - 明确 `TF ≡ rollout` 只对当前 `query_aug + feedback=none` baseline_v2 成立
2. 在 `cc_core_files/plan.md` 新增 `1.3.7`：
   - 固定 NeuroHorizon 默认数据协议：`train / valid / test` split、continuous / trial-aligned 两套 sampler 选择
   - 固定主指标、对照指标与 trial 指标的默认口径
   - 指明正式表格与曲线应优先引用 `eval_phase1_v2.py` 产生的离线累计结果
   - 约束 `1.8.x` benchmark 对比在引用 NeuroHorizon 结果时默认沿用该标准；faithful baseline 若不一致，必须在文档中显式注明
3. 在新标准中明确：
   - `1.3.5` 已按该标准补充 `ibl_mtm_bps` comparison metric，但不替代主 `fp-bps`
   - `1.3.6` 已按同一训练 / 评估协议执行，唯一核心变量为 `causal_decoder: false`

## 结果与判断

1. `plan.md` 中 baseline_v2 的“已完成事项”和“默认标准”现在分开表述：
   - `1.2.5` 负责补记已经落地的 review 吸收结果
   - `1.3.7` 负责定义后续实验默认协议
2. 当前主指标继续维持：
   - `global spike-weighted fp-bps + train-split null`
3. `ibl_mtm_bps` 继续定位为 comparison metric：
   - 可用于与 IBL-MtM 风格结果对照
   - 不替代主结论口径
4. `1.3.5` / `1.3.6` 现在都可以被明确引用为“遵循最新 continuous 主协议”的实验

## 遇到的问题与解决

- review 文档中的建议编号与本次补写重点容易被混淆  
  解决：不沿用建议编号表述，直接按“deterministic eval sampler”与“全局累计主指标”两个事实上链到 `plan.md`
- 需要让 `1.8.x` benchmark 对比线也能引用这套标准，但又不能误写成 external faithful baseline 已完全统一协议  
  解决：在 `1.3.7` 中明确“NeuroHorizon 结果默认遵循本标准；external benchmark 若协议不同，必须单列说明”
- `1.3.5` / `1.3.6` 以 continuous 主表为核心结论，不能夸大成已完整覆盖所有 trial 侧标准  
  解决：在 `1.3.7` 中把两者表述为“已按同一 continuous 主协议执行”，trial 指标单列为补充口径

## 追加记录（2026-03-18 17:29）

### 本次调整目标

1. 进一步收紧 `1.3.7` 的默认协议表述：
   - 默认实验只写 continuous
   - trial-aligned 改成可选分析路径
2. 进一步收紧指标默认口径：
   - 默认只写主 `fp-bps`
   - 其他指标全部降为可选补充项
3. 补充 `per-bin fp-bps` 在 long-horizon decay 比较中的用途与实现摘要

### 本次修改

1. 重写 `1.3.7` 的“默认数据与 dataloader 协议”：
   - 保留 `train / valid / test` split 与 continuous sampler 作为默认协议
   - 把 `TrialAlignedSampler` 改为“仅在 hold/reach 对齐分析时按需启用”，不再写成默认实验流程
2. 重写 `1.3.7` 的“默认指标与聚合协议”：
   - 明确默认正式结果只使用主 `global spike-weighted fp-bps + train-split null`
   - 把 `R-squared`、`ibl_mtm_bps`、`per_neuron_psth_r2` 都改为可选补充指标
3. 新增 `per-bin fp-bps` 说明：
   - 其定位是 long-horizon prediction decay 分析
   - 文中补充了 `fp_bps_per_bin_stats()` 与 `finalize_fp_bps_per_bin_from_stats()` 的简要实现逻辑
   - 并注明正式衰减曲线默认引用 `eval_phase1_v2.py` 的离线累计结果，而不是训练期 batch 级 logger

### 本次结果与判断

1. `1.3.7` 现在更明确地区分了：
   - 默认协议
   - 可选分析协议
   - comparison / diagnosis 指标
2. 默认实验口径被进一步收紧为：
   - continuous
   - 主 `fp-bps`
3. `per-bin fp-bps` 被保留为明确的 decay 分析工具，而不是默认主表指标
