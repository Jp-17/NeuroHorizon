# Phase 1.8: Benchmark 对比实验记录

> **日期**：2026-03-12
> **对应 plan.md**：Phase 1 → 1.8.1 / 1.8.2 / 1.8.3
> **执行状态**：进行中

---

## 2026-03-12 | 1.8.1 模型调研与文档整理

**完成内容**：

1. **创建 `neural-benchmark/` 目录结构**：
   ```
   neural-benchmark/
   ├── benchmark_model.md    # 模型调研文档
   ├── benchmark_models/     # 各模型代码仓库（待 clone）
   ├── adapters/             # 适配层代码（待开发）
   └── envs/                 # conda 环境存储（待配置）
   ```

2. **调研 NDT2 并整理到 benchmark_model.md**：
   - 架构：Encoder-Decoder Transformer, MAE, 约 3.7M 参数
   - FP 方式：并行填充（mask future bins → 重建）
   - 有预训练权重（wandb/GDrive），有运动皮层经验（RTT, FALCON）
   - 适配难度：低，约 100 行 adapter，1–2 天

3. **调研 Neuroformer 并整理到 benchmark_model.md**：
   - 架构：Decoder-only GPT-style, 约 8–100M 参数
   - FP 方式：逐 spike 自回归生成
   - 无运动皮层权重，需从零训练
   - 适配难度：中，约 150 行 adapter，2–3 天
   - 风险：从未在 electrophysiology 运动皮层测试

4. **调研 IBL-MtM 并整理到 benchmark_model.md**：
   - 架构：Encoder-only, Multi-task Masking, 约 3M–25.55M 参数
   - FP 方式：temporal causal masking + 并行预测
   - HuggingFace 权重需组织权限
   - 适配难度：中，约 120 行 adapter，2 天

5. **添加备选模型备注**：NEDS、STNDT、NDT3（均标注为"待评估"）

**产出文件**：
- `neural-benchmark/benchmark_model.md`（310 行，含完整调研信息）

**执行结果**：✅ 1.8.1 全部完成

**遇到的问题**：无

